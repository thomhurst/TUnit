<?xml version="1.0" encoding="utf-8"?>
<Project>
    <ItemGroup>
        <Files Include="$(MSBuildThisFileDirectory)..\lib\net6.0\*.dll" />
    </ItemGroup>
    <Target Name="CopyFiles" AfterTargets="Build">
        <Copy SourceFiles="@(Files)" DestinationFolder="$(TargetDir)" />
    </Target>
</Project>