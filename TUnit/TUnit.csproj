<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <IsLibraryPackage>true</IsLibraryPackage>
    <IsTestProject>false</IsTestProject>
    <IsTestingPlatformApplication>false</IsTestingPlatformApplication>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\TUnit.Assertions\TUnit.Assertions.csproj" />
    <ProjectReference Include="..\TUnit.Engine\TUnit.Engine.csproj" />
  </ItemGroup>

  <ItemGroup>
    <TfmSpecificPackageFile Include="../TUnit.Engine/TUnit.Engine.props">
      <Pack>true</Pack>
      <PackagePath>$(BuildPath)</PackagePath>
    </TfmSpecificPackageFile>

    <TfmSpecificPackageFile Include="../TUnit.Core/TUnit.Core.props">
      <Pack>true</Pack>
      <PackagePath>$(BuildPath)</PackagePath>
    </TfmSpecificPackageFile>

    <TfmSpecificPackageFile Include="../TUnit.Core/TUnit.Core.targets">
      <Pack>true</Pack>
      <PackagePath>$(BuildPath)</PackagePath>
    </TfmSpecificPackageFile>

    <TfmSpecificPackageFile Include="../TUnit.Assertions/TUnit.Assertions.props">
      <Pack>true</Pack>
      <PackagePath>$(BuildPath)</PackagePath>
    </TfmSpecificPackageFile>

    <TfmSpecificPackageFile Include="../TUnit.Assertions/TUnit.Assertions.targets">
      <Pack>true</Pack>
      <PackagePath>$(BuildPath)</PackagePath>
    </TfmSpecificPackageFile>
  </ItemGroup>

</Project>